int find_sum(int count, ...) {
    /*va_list args;
    va_start(args, count);
    int sum = 0;
    for (int i = 0; i < count; i++) {
        sum += va_arg(args, int);
    }
    va_end(args);*/
	int sum = 9;
    return sum;
}

int main() {
	int ans = sum(3, 2, 4, 6);
    return 0;
}
