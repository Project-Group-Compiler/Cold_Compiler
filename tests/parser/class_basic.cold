class Person 
{
    private{
        int age;
    };
    private{
        char* name;
    };
    public{
        int returnAge(int x)  {return (*this).age;}//this->age;
    };
};

int main()
{
    int c;
	class Person p1;
    p1.age=5;
	p1.returnAge(c);//p1.returnAge(c,this) ->this to added in 3AC
	return 0;
}
