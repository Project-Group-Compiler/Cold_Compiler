class Person 
{
    private{
        int age;
    };
    private{
        char* name;
    };
    public{
        int returnAge(int x)  {
            int y=x;
            return (*this).age;
        }//this->age;
        void solve(){
            (*this).returnAge(3);//TODO
        }
    };
    
        
};

int main()
{
    int c;
	class Person p1;
    p1.age=5;
	p1.returnAge(c);//p1.returnAge(c,this) ->this to added in 3AC
    p1.solve();
	return 0;
}
