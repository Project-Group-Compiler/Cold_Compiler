enum Color { RED, GREEN, BLUE };

void test_enum() {
    enum Color c = GREEN;
    if (c == 1) {
        // Test Passed
    }
}
union Data {
    int i;
    float f;
};

void test_union() {
    union Data d;
    d.i = 10;
    
    if (d.i == 10) {
        // Test Passed
    }
}
