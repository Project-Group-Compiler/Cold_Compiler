// SEMANTICS WORKING
// IR WORKING

struct Node {
    int val;
    struct Node* L;
    struct Node* R;
};

int main()
{
    struct Node aa;
    int a, b, c;
    char op;
    aa.val=5;
    aa.val++;
    ++aa.val;
    --aa.val;
    aa.val--;
    switch(op){
        case '+':
            c = a+b;
            printf("%d\n",c);
            break;
        case '-':
            c = a-b;
            printf("%d\n",c);
            break;
        default:
            printf("Not a valid operator\n");
    }
    return 0;
}
