// SEMANTICS WORKING
// IR WORKING

struct node{
    int x;
    int y;
    struct node* next;
};

int main()
{
    struct node* head, temp;
    // head = (struct node*)malloc(sizeof(struct node));
    // This malloc does not work
    head->x = 1;
    head->next = temp;
}