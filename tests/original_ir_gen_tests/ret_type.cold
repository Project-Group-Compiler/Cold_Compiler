// ERROR TESCASE -> NO IR GEN
// function return type when assigned to Variable Semantics workings
void add(int a, int b) {
    return a + b;
}
int sub(int a,int b){
    return a-b;
}
int main() {
    int x=5,y;
    char* a=add(x,y); // error recovery -> WORKING
    int c;
    char b="Hello";
    c=add(x,y);
    c=sub(x,y);
    return 0;
}
