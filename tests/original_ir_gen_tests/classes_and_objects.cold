// SEMANTICS WORKING
// IR WORKING

class Person
{
public{
        int age;
    };
public{
        int returnAge(int x)
        {
            return 42;
        }
    };
};

void returnAge(int x)
{
    return 42;
}

int main()
{
    int x;
    class Person p1;
    p1.age = 6;
    p1.returnAge(p1.age);
    returnAge(x);
    p1.returnAge(x);
    return 0;
}
