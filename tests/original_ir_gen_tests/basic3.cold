// IR WORKING
// SEMANTICS WORKING

// global variables
int g = 0;
int *ptr = &g;

void test_arith()
{
    int a = 6;
    int b = 3;
    int c = 2;
    int res1, res2, res3, res4, res5;

    res1 = a * b + c;
    res2 = a / b;
    res3 = a % c;

    res4 = a + b;
    res5 = a - c;
}

void test_others()
{
    int a = 5;
    int b = 3;
    int c = 2;

    int shiftLeft = a << 1;
    int shiftRight = b >> 1;
    int is_eq = (a == b);
    int and_res = a & c;
    int xor_res = a ^ b;
    int or_res = a | b;
    int complex_res = (a << 1) & (b | c) ^ (a >> 1);
    int max_ab = (a > b) ? a : b;
    b = -a;
    c = sizeof(a);
    ++a;
    --b;
    c += 7;//------------------
}

void test_bool()
{
    int a = 1, b = 0, x = 1, y = 1;

    if (x > 10)
        x = x - 1;
    else
        x = x + 2;

    if ((x && y) && (a && b))
        x = a + b;
}

void test_iteration()
{
    int i = 0, j, k;
    for (j = 0; j < 5; j++)
    {
        i += j;
    }
    return i;
}

int add(int a, int b)
{
    return a + b;
}

void call_add()
{
    int x;
    int y;
    x=1;
    y=1;
    add(x, y);
}